# ============================================================================
# REFERENCE ONLY — DO NOT EDIT
# ============================================================================
# This file is auto-generated by the SRE platform when your bundle is deployed.
# You do NOT need to create or modify this file. It's included here so you can
# see what the platform generates from your bundle.yaml.
#
# Your only job is to create bundle.yaml. The platform handles the rest.
# ============================================================================
---
# gitea — deployed via sre-deploy-app.sh
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitea
  namespace: team-test
  labels:
    app.kubernetes.io/part-of: sre-platform
    sre.io/team: team-test
spec:
  interval: 10m
  chart:
    spec:
      chart: ./apps/templates/web-app
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    app:
      name: "gitea"
      team: "team-test"
      image:
        repository: "harbor.apps.sre.example.com/team-test/gitea"
        tag: "v1.22-rootless"
        pullPolicy: IfNotPresent
      port: 3000
      replicas: 1
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 200m
          memory: 256Mi
      probes:
        liveness:
          path: "/"
          initialDelaySeconds: 10
          periodSeconds: 10
        readiness:
          path: "/"
          initialDelaySeconds: 5
          periodSeconds: 5
      env: []
    containerSecurityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: false
      runAsNonRoot: true
      capabilities:
        drop:
          - ALL
    startupProbe:
      enabled: true
      path: "/"
      initialDelaySeconds: 5
      periodSeconds: 5
      failureThreshold: 30
    persistence:
      enabled: true
      mountPath: "/var/lib/gitea"
      size: "10Gi"
    ingress:
      enabled: true
      host: "gitea.apps.sre.example.com"
    autoscaling:
      enabled: false
    serviceMonitor:
      enabled: false
    networkPolicy:
      enabled: true
    podDisruptionBudget:
      enabled: false
