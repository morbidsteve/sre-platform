# ============================================================================
# REFERENCE ONLY — DO NOT EDIT
# ============================================================================
# This file is auto-generated by the SRE platform when your bundle is deployed.
# You do NOT need to create or modify this file. It's included here so you can
# see what the platform generates from your bundle.yaml.
#
# Your only job is to create bundle.yaml. The platform handles the rest.
# ============================================================================
---
# order-worker — deployed via sre-deploy-app.sh
# Reference copy: this is what the deploy script generates for the worker component.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: order-worker
  namespace: team-demo
  labels:
    app.kubernetes.io/part-of: sre-platform
    sre.io/team: team-demo
spec:
  interval: 10m
  chart:
    spec:
      chart: ./apps/templates/worker
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    app:
      name: "order-worker"
      team: "team-demo"
      image:
        repository: "harbor.apps.sre.example.com/team-demo/order-worker"
        tag: "v1.0.0"
        pullPolicy: IfNotPresent
      port: 8080
      replicas: 1
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          cpu: 200m
          memory: 256Mi
      probes:
        liveness:
          path: "/"
          initialDelaySeconds: 10
          periodSeconds: 10
        readiness:
          path: "/"
          initialDelaySeconds: 5
          periodSeconds: 5
      env: []
    singleton: true
    ingress:
      enabled: false
    autoscaling:
      enabled: false
    serviceMonitor:
      enabled: false
    networkPolicy:
      enabled: true
    podDisruptionBudget:
      enabled: false
