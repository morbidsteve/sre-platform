# ============================================================================
# REFERENCE ONLY — DO NOT EDIT
# ============================================================================
# This file is auto-generated by the SRE platform when your bundle is deployed.
# You do NOT need to create or modify this file. It's included here so you can
# see what the platform generates from your bundle.yaml.
#
# Your only job is to create bundle.yaml. The platform handles the rest.
# ============================================================================
---
# order-api — deployed via sre-deploy-app.sh
# Reference copy: this is what the deploy script generates for the API component.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: order-api
  namespace: team-demo
  labels:
    app.kubernetes.io/part-of: sre-platform
    sre.io/team: team-demo
spec:
  interval: 10m
  chart:
    spec:
      chart: ./apps/templates/api-service
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    app:
      name: "order-api"
      team: "team-demo"
      image:
        repository: "harbor.apps.sre.example.com/team-demo/order-api"
        tag: "v1.0.0"
        pullPolicy: IfNotPresent
      port: 8080
      replicas: 1
      resources:
        requests:
          cpu: 250m
          memory: 256Mi
        limits:
          cpu: 1000m
          memory: 1Gi
      probes:
        liveness:
          path: "/healthz"
          initialDelaySeconds: 10
          periodSeconds: 10
        readiness:
          path: "/readyz"
          initialDelaySeconds: 5
          periodSeconds: 5
      env: []
    ingress:
      enabled: true
      host: "orders.apps.sre.example.com"
    autoscaling:
      enabled: false
    serviceMonitor:
      enabled: true
    networkPolicy:
      enabled: true
    podDisruptionBudget:
      enabled: false
