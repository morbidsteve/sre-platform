---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-env-values
  namespace: tempo
data:
  values.yaml: |
    persistence:
      enabled: true
      storageClassName: local-path
      size: 10Gi
