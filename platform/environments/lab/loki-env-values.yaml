---
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-env-values
  namespace: logging
data:
  values.yaml: |
    singleBinary:
      persistence:
        enabled: true
        storageClass: local-path
        size: 20Gi
