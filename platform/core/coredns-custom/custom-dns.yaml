---
# CoreDNS custom DNS entries for *.apps.sre.example.com
# This allows in-cluster pods to resolve the domain, but nodes still need
# /etc/hosts or a real DNS server.
#
# For node-level resolution (containerd image pulls), we use a DaemonSet
# that adds entries to the node /etc/hosts file.
apiVersion: v1
kind: ConfigMap
metadata:
  name: node-hosts-config
  namespace: kube-system
  labels:
    app.kubernetes.io/part-of: sre-platform
data:
  hosts-entries: |
    # SRE Platform — MetalLB gateway IP for all *.apps.sre.example.com services
    192.168.2.200 harbor.apps.sre.example.com
    192.168.2.200 dashboard.apps.sre.example.com
    192.168.2.200 grafana.apps.sre.example.com
    192.168.2.200 keycloak.apps.sre.example.com
    192.168.2.200 neuvector.apps.sre.example.com
    192.168.2.200 openbao.apps.sre.example.com
    192.168.2.200 oauth2.apps.sre.example.com
    192.168.2.200 portal.apps.sre.example.com
    192.168.2.200 prometheus.apps.sre.example.com
    192.168.2.200 alertmanager.apps.sre.example.com
  registries-yaml: |
    mirrors:
      harbor.apps.sre.example.com:
        endpoint:
          - "https://harbor.apps.sre.example.com"
    configs:
      "harbor.apps.sre.example.com":
        tls:
          insecure_skip_verify: true
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: node-hosts-manager
  namespace: kube-system
  labels:
    app.kubernetes.io/name: node-hosts-manager
    app.kubernetes.io/part-of: sre-platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: node-hosts-manager
  template:
    metadata:
      labels:
        app.kubernetes.io/name: node-hosts-manager
        app.kubernetes.io/part-of: sre-platform
    spec:
      hostNetwork: true
      tolerations:
        - operator: Exists
      containers:
        - name: hosts-manager
          image: docker.io/library/busybox:1.36.1
          command:
            - /bin/sh
            - -c
            - |
              # Add SRE platform hosts entries if not already present
              MARKER="# SRE-PLATFORM-MANAGED"
              while true; do
                if ! grep -q "$MARKER" /host-etc/hosts; then
                  echo "" >> /host-etc/hosts
                  echo "$MARKER" >> /host-etc/hosts
                  cat /config/hosts-entries >> /host-etc/hosts
                  echo "Added SRE platform hosts entries"
                fi
                # Write RKE2 registries.yaml for Harbor TLS trust
                REGISTRIES="/host-rancher/registries.yaml"
                if [ ! -f "$REGISTRIES" ] || ! grep -q "harbor.apps.sre.example.com" "$REGISTRIES" 2>/dev/null; then
                  mkdir -p /host-rancher
                  cp /config/registries-yaml "$REGISTRIES"
                  echo "Wrote RKE2 registries.yaml (RKE2 restart needed to pick up)"
                fi
                sleep 3600
              done
          volumeMounts:
            - name: host-etc
              mountPath: /host-etc
            - name: host-rancher
              mountPath: /host-rancher
            - name: config
              mountPath: /config
          securityContext:
            runAsUser: 0
            privileged: true
          resources:
            requests:
              cpu: 5m
              memory: 8Mi
            limits:
              cpu: 10m
              memory: 16Mi
      volumes:
        - name: host-etc
          hostPath:
            path: /etc
        - name: host-rancher
          hostPath:
            path: /etc/rancher/rke2
        - name: config
          configMap:
            name: node-hosts-config
