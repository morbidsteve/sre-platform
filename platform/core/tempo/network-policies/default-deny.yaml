---
# Default deny all ingress and egress for tempo namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: tempo
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
