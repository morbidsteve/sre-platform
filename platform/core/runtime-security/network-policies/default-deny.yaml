---
# Default deny all ingress and egress for neuvector namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: neuvector
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
