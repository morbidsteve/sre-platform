---
# Default deny all ingress and egress for velero namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: velero
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
