---
# Default deny all ingress and egress for backstage namespace
# All traffic must be explicitly allowed by other NetworkPolicies
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: backstage
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
