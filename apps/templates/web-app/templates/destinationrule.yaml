{{/*
  DestinationRule is not needed for HTTPS backends.
  Istio mTLS STRICT handles sidecar-to-sidecar encryption.
  For HTTPS backends, the service port is named "tcp" for opaque TCP passthrough
  so the gateway forwards raw TCP to the sidecar, which forwards to the app.
*/}}
