{{- if .Values.networkPolicy.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sre-worker.fullname" . }}
  labels:
    {{- include "sre-worker.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sre-worker.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    {{- if .Values.serviceMonitor.enabled }}
    {{- include "sre-lib.networkpolicy-ingress-monitoring" .Values.serviceMonitor.port | nindent 4 }}
    {{- end }}
  egress:
    {{- include "sre-lib.networkpolicy-egress" . | nindent 4 }}
{{- end }}
