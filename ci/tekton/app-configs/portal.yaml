---
# Manual PipelineRun: SRE Portal
# Use this to manually trigger a portal build without a webhook.
#
# Usage:
#   kubectl create -f ci/tekton/app-configs/portal.yaml
#
# Before running, update the image-tag parameter to the desired version.
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: portal-manual-
  namespace: tekton-pipelines
  labels:
    app.kubernetes.io/part-of: sre-platform
    tekton.dev/pipeline: build-deploy
    sre.io/app: sre-portal
    sre.io/trigger: manual
spec:
  pipelineRef:
    name: build-deploy
  params:
    - name: git-url
      value: "https://github.com/morbidsteve/sre-platform.git"
    - name: git-revision
      value: "main"
    - name: app-name
      value: "sre-portal"
    - name: image-name
      value: "sre-portal"
    - name: image-tag
      value: "v2.3.1"
    - name: dockerfile-path
      value: "Dockerfile"
    - name: build-context
      value: "apps/portal"
    - name: deployment-file
      value: "apps/portal/k8s/deployment.yaml"
    - name: working-dir
      value: "apps/portal"
    - name: validation-type
      value: "typescript"
  workspaces:
    - name: shared-workspace
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
    - name: docker-credentials
      secret:
        secretName: harbor-push-creds
    - name: git-credentials
      secret:
        secretName: github-token
  taskRunTemplate:
    podTemplate:
      securityContext:
        fsGroup: 65532
