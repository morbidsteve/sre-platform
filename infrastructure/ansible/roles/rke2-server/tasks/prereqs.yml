---
# RKE2 server-specific prerequisites
# Common prerequisites (packages, NetworkManager, directories) are handled
# by the rke2-common role dependency.

- name: Create etcd user for CIS compliance
  ansible.builtin.user:
    name: etcd
    system: true
    comment: "etcd user"
    shell: /sbin/nologin
    create_home: false
