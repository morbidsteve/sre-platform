# Proxmox Module — Cloud-Init Configuration
# Uses inline cloud-init via the Proxmox API (no SSH required for snippet uploads).
# NIST Controls: IA-5 (authenticator management), AC-17 (remote access)
